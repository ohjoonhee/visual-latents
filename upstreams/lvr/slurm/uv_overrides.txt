# uv override for two unresolvable pins in the (otherwise pristine) upstream
# requirements.txt — applied via `uv pip install --override` so requirements.txt
# stays byte-identical to upstream (faithfulness).
#
#  - av==14.3.0 was YANKED from PyPI (zero files for that exact version under any
#    Python); nearest available patch is 14.4.0. (av is PyAV, video-decode — not on
#    the LVR image path, but it's a frozen transitive dep so it must resolve.)
#  - huggingface-hub==0.30.2 contradicts transformers==4.54.0, which requires
#    hub>=0.34.0,<1.0. The 0.30.2 line is stale in the upstream freeze; bumping lets
#    the set resolve (transformers pulls hub 0.36.x). The authors' real working env
#    could not have had both, so this restores consistency rather than diverging.
#
# Validated by `uv pip install --dry-run -r requirements.txt --override <this> boto3`
# under both py3.11 and py3.12: full set resolves with torch 2.6.0 / transformers
# 4.54.0 / deepspeed 0.16.7 / trl 0.17.0 all intact.
av==14.4.0
huggingface-hub>=0.34.0,<1.0
