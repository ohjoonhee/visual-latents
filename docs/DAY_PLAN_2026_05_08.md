# Day Plan — 2026-05-08

## Context

Phase 1.5 + Phase 2 finished overnight. Both reached marginal/failing verdicts,
but with two known methodological gaps that prevent clean interpretation:

1. **Phase 1.5 did not actually pass `attention_mask_4d`** — the very mechanism
   the test was designed to probe (Monet's cross-slot isolation). It tested
   only Monet's recurrence pattern.
2. **Phase 2 evaluated out-of-distribution** — trained on Monet's `Visual_CoT`
   (with `<observation>` tags & aux images), evaluated on
   `ohjoonhee/visual-cot-50k-poc` (no `<observation>`). The harmful-utility
   reading is contaminated by this mismatch.

The cosine signal from Phase 2 (mean=0.737) sits between Monet stage 2 (0.38)
and stage 3 (0.85) — suggestive but inconclusive. Before pivoting, we close
both methodological gaps.

## Schedule (~10h budget)

| # | Experiment | Hypothesis | Cost | Priority |
|---|---|---|---:|---|
| A | Phase 2 in-distribution re-eval | Harmful-utility was OOD artifact, not collapse | ~30-60 min | High (cheapest) |
| B | Phase 1.5b with `attention_mask_4d` injected | Cross-slot attention isolation breaks LVR collapse | ~2-3h | High (closes original gap) |
| C | Pivot A POC at 3B (LVR + cosine-penalty regularizer) | Direct regularization breaks collapse without attention mask | ~3-4h | High (project's natural main thread) |
| D | Phase 2b (add `attention_mask_4d` + `emphasize_latent_weight` from B) | The remaining quick deviations matter | ~2-3h | Conditional on time |
| F | Final synthesis report + recommendation | n/a | ~30 min | After all |

Each experiment writes a self-contained `REPORT.md` in its own directory.
After each one, code + report are committed and pushed to `origin/main` so the
user can spot-check during the day.

## Success criteria (per experiment)

- **A:** "Verdict flip" — Phase 2 in-distribution mean cos < 0.55 AND utility > 0
  on Monet held-out → distribution mismatch was the failure cause. Otherwise:
  collapse is real, recipe gap (no Stage 1 SFT, no `attention_mask_4d`, etc.) is
  the cause.
- **B:** Same 4 acceptance criteria as Phase 1.5 (`compression_ratio ≥ 0.4`,
  `mean_off_diag_cos ≤ 0.55`, `n_helpful ≥ 3`, `qwen_base utility > 0`). PASS
  ≥ 3/4 → cross-slot isolation IS the load-bearing mechanism. FAIL → it
  isn't, attention mask alone doesn't rescue per-position differentiation at
  3B/5K.
- **C:** Same 4 criteria. PASS → simple regularization works at 3B/5K, project
  natural main thread is viable. FAIL → 3B/5K is fundamentally too small to
  observe distributed latents under any current recipe.
- **D:** Same 4 criteria evaluated in-distribution per A's recipe.

## Decision matrix (at end of day)

| A | B | C | D | Direction |
|---|---|---|---|---|
| flip | pass | pass | n/a | Strong positive: pivot A is the natural choice; Phase 2 also works in-dist; promote to cluster Phase 3 |
| flip | fail | pass | n/a | Direct regularization works without attention mask; pivot A v2 at 3B is the path |
| flip | pass | fail | n/a | Attention mask is the load-bearing piece; cluster Phase 3 with full Monet stage 2 |
| no flip | pass | pass | optional | Both mechanisms recovered; pick the simpler (pivot A) for next iteration |
| no flip | fail | fail | optional | 3B/5K is fundamentally too small; escalate to cluster Phase 3 OR write up the diagnostic |

## Sync cadence

- Initial plan commit (this doc + tasks)
- After A: commit `phase2_indist/`
- After B: commit `phase1_5b_attn/`
- After C: commit `pivot_a/`
- After D (if run): commit `phase2b_monet_stage2/`
- Final synthesis: commit `docs/DAY_REPORT_2026_05_08.md`

All on `main`. No branches — these are independent additive directories.

## Operational notes (carried from prior phases)

- Use the pinned `phase0_monet_probe/.venv-monet` (transformers 4.54.0).
- Reuse `phase0_monet_probe/ablation.py` for all ablation runs (consistency).
- For in-distribution Phase 2 eval (Experiment A), data lives at
  `phase0_monet_probe/data/Monet-SFT-125K/Visual_CoT/`.
- Phase 2 checkpoint: `phase2_monet_stage2/results/<run_name>/` (re-confirm path
  before re-extraction).
